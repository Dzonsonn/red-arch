export interface Usluga {
    ime: string;
    paketi: Paket[];
    slika: string;
  }
  
  export interface Paket {
    naziv: string;
    cena: number;
  }
 export interface Service {
    image: string;
    title: string;
  }